<%= form_with model: [list, bookmark] do |f| %>
  <%= f.label :movie_id, "Film" %>
  <%= f.collection_select :movie_id, Movie.all, :id, :title, prompt: "Choisir un film" %>

  <%= f.label :comment %>
  <%= f.text_field :comment %>

  <%= f.submit "Ajouter" %>
<% end %>
